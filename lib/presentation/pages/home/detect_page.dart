import 'package:flutter/material.dart';

class DetectPage extends StatelessWidget {
  const DetectPage({super.key});
  @override Widget build(BuildContext c)=>const Center(child:Text('Pose Detect'));
}